import { basicsStyles, CSSPropAdapted } from "~utils/stylesheets";

const CSSHome: CSSPropAdapted = {
  container: {
    ...basicsStyles.flexCenter,
    flexDirection: "column",
  },
};

export default CSSHome;
