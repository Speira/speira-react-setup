import { basicsStyles, CSSPropAdapted } from "~utils/css.utils";

const homeCSS: CSSPropAdapted = {
  container: {
    ...basicsStyles.flexCenter,
    flexDirection: "column",
  },
};

export default homeCSS;
